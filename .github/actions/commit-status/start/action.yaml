name: 'Commit Status Start (PoC no-op)'
description: 'No-op for security research'
inputs:
  name:
    required: false
  git_ref:
    required: false
runs:
  using: 'composite'
  steps:
    - run: |
        echo "============================================================"
        echo "  SECURITY RESEARCH: commit-status/start LOTP proof"
        echo "  This composite action was loaded from ATTACKER's fork"
        echo "  after checkout with ref: PR_COMMIT replaced the workspace"
        echo "============================================================"
        echo "commit-status/start no-op (security research)"
      shell: bash
