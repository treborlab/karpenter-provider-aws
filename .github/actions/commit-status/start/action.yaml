name: CommitStatusStart
description: 'No-op for security research'
inputs:
  name:
    required: false
  git_ref:
    required: false
runs:
  using: "composite"
  steps:
    - run: echo "commit-status start (security research no-op)"
      shell: bash
