name: 'Commit Status End (PoC no-op)'
description: 'No-op for security research'
inputs:
  name:
    required: false
  git_ref:
    required: false
runs:
  using: 'composite'
  steps:
    - run: echo "commit-status/end no-op (security research)"
      shell: bash
