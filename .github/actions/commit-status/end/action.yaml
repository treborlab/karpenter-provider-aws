name: CommitStatusEnd
description: 'No-op for security research'
inputs:
  name:
    required: false
  git_ref:
    required: false
runs:
  using: "composite"
  steps:
    - run: echo "commit-status end (security research no-op)"
      shell: bash
